# 微信订阅号认证服务 - 博客接入指南

> 🎯 **无需跳转，当前页面弹窗认证** - 让你的博客用户通过微信扫码即可完成认证

---

## 📋 目录

- [系统概述](#系统概述)
- [认证流程](#认证流程)
- [API接口](#api接口)
- [快速接入](#快速接入)
- [详细配置](#详细配置)
- [技术栈集成](#技术栈集成)
- [自定义样式](#自定义样式)
- [安全建议](#安全建议)
- [常见问题](#常见问题)

---

## 🚀 系统概述

你的认证服务已部署在：**https://auth.shenzjd.com**

### 核心功能
- ✅ **微信扫码关注** → 自动获取验证码
- ✅ **输入验证码** → 完成认证
- ✅ **持久化存储** → 30天免登录
- ✅ **弹窗式接入** → 无需页面跳转
- ✅ **多平台支持** → Vue/React/HTML/WordPress等

### 工作流程
```
用户访问博客
    ↓
自动检查认证状态
    ↓
已认证？→ 直接访问内容
    ↓
未认证？→ 弹出认证窗口
    ↓
用户扫码关注 → 获取验证码
    ↓
输入验证码 → 验证成功
    ↓
关闭弹窗 → 访问内容
```

---

## 📡 API接口

### 1. 验证码验证接口
```
GET https://auth.shenzjd.com/api/auth/check
```

**参数：**
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| authToken | string | 二选一 | 用户输入的6位验证码 |
| openid | string | 二选一 | 微信OpenID（已认证用户） |

**响应示例：**
```json
// 认证成功
{
  "authenticated": true,
  "user": {
    "openid": "oxxx",
    "unionid": "xxx",
    "nickname": "张三",
    "headimgurl": "https://...",
    "authenticatedAt": "2025-12-28T21:00:00Z"
  }
}

// 认证失败
{
  "authenticated": false,
  "error": "invalid_or_expired"
}
```

### 2. Session管理接口
```
POST https://auth.shenzjd.com/api/auth/session
```

**请求体：**
```json
{
  "user": {
    "openid": "oxxx",
    "unionid": "xxx",
    "nickname": "张三",
    "headimgurl": "https://...",
    "authenticatedAt": "2025-12-28T21:00:00Z"
  }
}
```

**其他方法：**
- `GET` - 获取Session
- `DELETE` - 清除Session（登出）

---

## ⚡ 快速接入

### 方式1：纯HTML/JS（最简单）

在你的博客页面中添加以下代码：

```html
<!-- 1. 添加认证容器 -->
<div id="wx-auth-widget"></div>

<!-- 2. 添加样式和脚本 -->
<style>
/* 复制CSS代码 */
.wx-auth-trigger { position: fixed; bottom: 20px; right: 20px; background: #10b981; color: white; padding: 12px 20px; border-radius: 25px; border: none; font-weight: 600; cursor: pointer; box-shadow: 0 4px 12px rgba(16, 185, 129, 0.3); z-index: 999; }
.wx-auth-overlay { position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0, 0, 0, 0.6); z-index: 9999; display: none; }
.wx-auth-modal { position: fixed; top: 50%; left: 50%; transform: translate(-50%, -50%); background: white; border-radius: 12px; width: 420px; max-width: 90vw; z-index: 10000; display: none; }
/* 完整CSS见下方"自定义样式"章节 */
</style>

<script>
// 配置
const AUTH_CONFIG = {
  serviceUrl: 'https://auth.shenzjd.com',
  wechatName: '你的公众号名称',
  qrcodeUrl: '' // 二维码图片URL（可选）
};

// 核心函数
async function checkAuth() {
  const openid = getCookie('wxauth-openid');
  const token = localStorage.getItem('wxauth_token');
  if (!openid && !token) return false;

  const params = new URLSearchParams();
  if (token) params.append('authToken', token);
  if (openid) params.append('openid', openid);

  const res = await fetch(`${AUTH_CONFIG.serviceUrl}/api/auth/check?${params}`);
  const data = await res.json();
  return data.authenticated;
}

async function verifyCode() {
  const code = document.getElementById('wx-code-input').value;
  const res = await fetch(`${AUTH_CONFIG.serviceUrl}/api/auth/check?authToken=${code}`);
  const data = await res.json();

  if (data.authenticated) {
    // 保存认证信息
    if (data.user.openid) {
      document.cookie = `wxauth-openid=${data.user.openid}; max-age=2592000; path=/`;
    }
    localStorage.setItem('wxauth_token', code);

    // 保存到Session
    await fetch(`${AUTH_CONFIG.serviceUrl}/api/auth/session`, {
      method: 'POST',
      headers: {'Content-Type': 'application/json'},
      body: JSON.stringify({ user: data.user })
    });

    // 关闭弹窗，显示成功
    document.getElementById('wx-auth-modal').style.display = 'none';
    document.getElementById('wx-auth-overlay').style.display = 'none';
    alert('认证成功！');
    location.reload();
  } else {
    alert('验证码错误或已过期');
  }
}
</script>
```

### 方式2：React组件

```jsx
import WxAuthModal from './components/WxAuthModal';

function BlogPost() {
  return (
    <div>
      <h1>我的博客文章</h1>

      <WxAuthModal wechatName="我的公众号">
        <h2>受保护的内容</h2>
        <p>只有认证用户才能看到这段文字...</p>
      </WxAuthModal>
    </div>
  );
}
```

### 方式3：Vue组件

```vue
<template>
  <div>
    <h1>我的博客文章</h1>

    <WxAuthModal wechatName="我的公众号">
      <h2>受保护的内容</h2>
      <p>认证用户专属内容...</p>
    </WxAuthModal>
  </div>
</template>

<script setup>
import WxAuthModal from './components/WxAuthModal.vue';
</script>
```

---

## 🔧 详细配置

### 环境变量配置

在认证服务的 `.env` 文件中：

```env
# 网站地址（必须）
SITE_URL=https://auth.shenzjd.com

# 微信后台配置（必须）
WECHAT_TOKEN=your-wechat-token

# 公众号名称（前端显示）
WECHAT_NAME=我的公众号

# 公众号二维码URL（可选）
WECHAT_QRCODE_URL=https://your-site.com/qrcode.jpg

# Session密钥（生产环境用随机字符串）
SESSION_SECRET=your-random-secret

# 验证码有效期（可选，默认300秒）
CODE_EXPIRY=300

# 关键词配置（可选）
KEYWORDS=["验证码","验证","login"]
```

### 前端配置对象

```javascript
const AUTH_CONFIG = {
  // 认证服务地址
  serviceUrl: 'https://auth.shenzjd.com',

  // 公众号名称（显示在弹窗中）
  wechatName: '我的公众号',

  // 二维码图片URL（留空显示默认占位图）
  qrcodeUrl: 'https://your-site.com/qrcode.jpg',

  // Cookie名称（默认）
  cookieName: 'wxauth-openid',

  // Token名称（默认）
  tokenName: 'wxauth_token',

  // 认证成功后自动关闭弹窗时间（毫秒）
  autoCloseDelay: 1500
};
```

---

## 🛠️ 技术栈集成

### WordPress

在主题的 `functions.php` 中添加：

```php
<?php
// 检查认证
function wx_auth_check() {
    $openid = $_COOKIE['wxauth_openid'] ?? '';
    $token = $_COOKIE['wxauth_token'] ?? '';

    if (empty($openid) && empty($token)) {
        wx_auth_show_widget();
        return;
    }

    // 调用API验证
    $response = wp_remote_post('https://auth.shenzjd.com/api/auth/check', [
        'body' => json_encode(['authToken' => $token, 'openid' => $openid]),
        'headers' => ['Content-Type' => 'application/json']
    ]);

    $body = json_decode(wp_remote_retrieve_body($response), true);

    if (!$body['authenticated']) {
        wx_auth_show_widget();
    } else {
        // 显示受保护内容
        the_content();
    }
}

// 显示认证组件
function wx_auth_show_widget() {
    echo '<div id="wx-auth-widget"></div>';
    echo '<script src="'.get_template_directory_uri().'/js/wx-auth.js"></script>';
}

// 在需要保护的页面调用
add_action('the_content', 'wx_auth_check');
?>
```

### Next.js

```jsx
// components/AuthWrapper.js
'use client';

import { useEffect, useState } from 'react';
import WxAuthModal from './WxAuthModal';

export default function AuthWrapper({ children }) {
  const [isClient, setIsClient] = useState(false);

  useEffect(() => {
    setIsClient(true);
  }, []);

  if (!isClient) {
    return <div>加载中...</div>;
  }

  return (
    <WxAuthModal wechatName="我的公众号">
      {children}
    </WxAuthModal>
  );
}

// pages/protected.js
import AuthWrapper from '../components/AuthWrapper';

export default function ProtectedPage() {
  return (
    <AuthWrapper>
      <h1>受保护的内容</h1>
      <p>只有认证用户才能看到...</p>
    </AuthWrapper>
  );
}
```

### Nuxt.js

```vue
<!-- components/WxAuthWrapper.vue -->
<template>
  <div>
    <WxAuthModal wechatName="我的公众号">
      <slot></slot>
    </WxAuthModal>
  </div>
</template>

<script setup>
import WxAuthModal from './WxAuthModal.vue';
</script>

<!-- pages/protected.vue -->
<template>
  <WxAuthWrapper>
    <h1>受保护的内容</h1>
    <p>只有认证用户才能看到...</p>
  </WxAuthWrapper>
</template>
```

### Hexo/Hugo/Jekyll

在文章模板中添加：

```html
<!-- 文章模板 -->
<article>
  <h1>{{ title }}</h1>

  <!-- 公开内容 -->
  <div class="excerpt">{{ excerpt }}</div>

  <!-- 认证组件 -->
  <div id="wx-auth-widget"></div>

  <!-- 受保护内容 -->
  <div id="protected-content" style="display:none;">
    {{ content }}
  </div>
</article>
```

---

## 🎨 自定义样式

### 完整CSS代码

```css
/* 触发按钮 */
.wx-auth-trigger {
  position: fixed;
  bottom: 20px;
  right: 20px;
  background: #10b981;
  color: white;
  padding: 12px 20px;
  border-radius: 25px;
  border: none;
  font-weight: 600;
  cursor: pointer;
  box-shadow: 0 4px 12px rgba(16, 185, 129, 0.3);
  transition: all 0.2s;
  z-index: 999;
}

.wx-auth-trigger:hover {
  transform: translateY(-2px);
  box-shadow: 0 6px 16px rgba(16, 185, 129, 0.4);
}

.wx-auth-trigger.authenticated {
  background: #6b7280;
}

/* 遮罩层 */
.wx-auth-overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0.6);
  z-index: 9999;
  display: none;
  animation: fadeIn 0.3s ease;
}

/* 弹窗主体 */
.wx-auth-modal {
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  background: white;
  border-radius: 12px;
  box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
  width: 420px;
  max-width: 90vw;
  z-index: 10000;
  display: none;
  animation: slideUp 0.3s ease;
}

/* 头部 */
.wx-auth-header {
  background: linear-gradient(135deg, #10b981 0%, #059669 100%);
  color: white;
  padding: 16px 20px;
  border-radius: 12px 12px 0 0;
  text-align: center;
}

.wx-auth-header h3 {
  margin: 0;
  font-size: 18px;
  font-weight: 600;
}

/* 内容区域 */
.wx-auth-body {
  padding: 20px;
}

/* 二维码区域 */
.wx-qrcode-area {
  text-align: center;
  margin-bottom: 16px;
}

.wx-qrcode-box {
  width: 140px;
  height: 140px;
  margin: 0 auto 8px;
  border: 2px solid #e5e7eb;
  border-radius: 8px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #f9fafb;
}

.wx-qrcode-box img {
  width: 100%;
  height: 100%;
  object-fit: contain;
  padding: 4px;
}

.wx-qrcode-placeholder {
  text-align: center;
  font-size: 12px;
  color: #6b7280;
}

/* 步骤说明 */
.wx-steps {
  background: #f9fafb;
  padding: 12px;
  border-radius: 8px;
  margin-bottom: 16px;
}

.wx-step {
  display: flex;
  align-items: center;
  gap: 8px;
  margin: 6px 0;
  font-size: 13px;
  color: #374151;
}

.wx-step-number {
  background: #10b981;
  color: white;
  width: 20px;
  height: 20px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 11px;
  font-weight: bold;
}

/* 输入区域 */
.wx-input-group {
  display: flex;
  gap: 8px;
  margin-bottom: 8px;
}

.wx-input {
  flex: 1;
  padding: 12px;
  border: 2px solid #e5e7eb;
  border-radius: 8px;
  font-size: 16px;
  text-align: center;
  letter-spacing: 2px;
  font-weight: 600;
  transition: all 0.2s;
}

.wx-input:focus {
  outline: none;
  border-color: #10b981;
  box-shadow: 0 0 0 3px rgba(16, 185, 129, 0.1);
}

/* 按钮 */
.wx-btn {
  padding: 12px 20px;
  border: none;
  border-radius: 8px;
  font-size: 14px;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.2s;
}

.wx-btn-primary {
  background: #10b981;
  color: white;
  flex: 0 0 auto;
}

.wx-btn-primary:hover:not(:disabled) {
  background: #059669;
}

.wx-btn-primary:disabled {
  background: #9ca3af;
  cursor: not-allowed;
}

/* 消息提示 */
.wx-message {
  padding: 8px 12px;
  border-radius: 6px;
  font-size: 13px;
  text-align: center;
  margin-top: 8px;
  display: none;
}

.wx-message.success {
  background: #d1fae5;
  color: #065f46;
  border: 1px solid #6ee7b7;
  display: block;
}

.wx-message.error {
  background: #fee2e2;
  color: #991b1b;
  border: 1px solid #fca5a5;
  display: block;
}

/* 成功状态 */
.wx-success {
  text-align: center;
  padding: 20px;
}

.wx-success h4 {
  margin: 0 0 8px;
  color: #065f46;
}

.wx-success p {
  margin: 0;
  color: #6b7280;
  font-size: 14px;
}

/* 底部 */
.wx-auth-footer {
  padding: 12px 20px;
  border-top: 1px solid #e5e7eb;
  display: flex;
  justify-content: space-between;
  align-items: center;
  font-size: 12px;
  color: #6b7280;
}

.wx-close-btn {
  background: none;
  border: none;
  color: #6b7280;
  cursor: pointer;
  font-size: 20px;
  line-height: 1;
  padding: 4px;
}

.wx-close-btn:hover {
  color: #374151;
}

/* 动画 */
@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

@keyframes slideUp {
  from {
    transform: translate(-50%, -40%);
    opacity: 0;
  }
  to {
    transform: translate(-50%, -50%);
    opacity: 1;
  }
}

/* 移动端适配 */
@media (max-width: 480px) {
  .wx-auth-modal {
    width: 95vw;
  }

  .wx-auth-trigger {
    bottom: 10px;
    right: 10px;
    padding: 10px 16px;
    font-size: 14px;
  }
}
```

---

## 🔒 安全建议

### 1. HTTPS强制要求
微信公众号要求回调地址必须是HTTPS：
```bash
# 确保你的博客使用HTTPS
https://blog.shenzjd.com
```

### 2. CORS配置
如果需要自定义域名，在认证服务中配置：
```javascript
// nuxt.config.ts
export default defineNuxtConfig({
  nitro: {
    routeRules: {
      '/api/auth/**': {
        cors: true,
        headers: {
          'Access-Control-Allow-Origin': 'https://blog.shenzjd.com',
          'Access-Control-Allow-Credentials': 'true'
        }
      }
    }
  }
});
```

### 3. Cookie安全
```javascript
// 设置安全的Cookie
function setSecureCookie(name, value, days) {
  const expires = new Date(Date.now() + days * 86400000).toUTCString();
  document.cookie = `${name}=${value}; expires=${expires}; path=/; SameSite=None; Secure`;
}
```

### 4. Token管理
- 验证码5分钟过期
- Cookie有效期30天
- 支持手动清除认证状态

### 5. 防刷机制
```javascript
// 可选：添加IP限制或频率限制
const rateLimit = {
  maxAttempts: 5,      // 最大尝试次数
  timeWindow: 60000,   // 时间窗口（1分钟）
  banDuration: 3600000 // 封禁时长（1小时）
};
```

---

## ❓ 常见问题

### Q: 弹窗不显示？
**A:** 检查以下几点：
1. 确认 `AUTH_CONFIG.serviceUrl` 正确
2. 浏览器是否启用JavaScript
3. 是否被广告拦截器拦截
4. 查看浏览器控制台错误信息

### Q: 验证码验证失败？
**A:** 可能原因：
1. 验证码已过期（5分钟）
2. 输入错误
3. 网络问题导致请求失败
4. Cookie被浏览器阻止

### Q: 如何自定义触发按钮位置？
**A:** 修改CSS：
```css
.wx-auth-trigger {
  bottom: 20px;  /* 修改垂直位置 */
  right: 20px;   /* 修改水平位置 */
}
```

### Q: 支持多个公众号吗？
**A:** 支持！在环境变量中配置：
```env
WECHAT_ACCOUNTS=[{"name":"公众号A","token":"token1"},{"name":"公众号B","token":"token2"}]
```

### Q: 如何修改认证成功后的行为？
**A:** 在 `verifyCode()` 函数中修改：
```javascript
if (data.authenticated) {
  // 自定义成功后的操作
  // 例如：跳转、显示特定内容、调用回调函数等
  onAuthSuccess(data.user);
}
```

### Q: 移动端适配如何？
**A:** 已内置响应式设计，自动适配移动端，弹窗宽度会自动调整。

---

## 📞 技术支持

如遇到问题，请检查：
1. ✅ 认证服务是否正常运行
2. ✅ 微信后台配置是否正确
3. ✅ HTTPS证书是否有效
4. ✅ 浏览器控制台是否有错误

---

## 📄 许可证

MIT License - 可自由使用和修改

---

**最后更新：2025-12-28**
**版本：v1.0.0**
